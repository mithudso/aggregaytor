{
  "name": "@aggregaytor/grindr-lib",
  "version": "0.1.0",
  "type": "module",
  "description": "Self-contained Grindr Web client (auth, blocks, limiter, chat, profiles, albums, DOM + compose helpers, traffic observer). Vendored verbatim from the Grindr Middle-Click Block userscript's lib/.",
  "main": "./src/index.js",
  "module": "./src/index.js",
  "types": "./src/index.d.ts",
  "exports": {
    ".": {
      "types": "./src/index.d.ts",
      "import": "./src/index.js"
    }
  },
  "files": ["src"],
  "scripts": {
    "build": "true",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.1.0"
  }
}
