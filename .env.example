# Aggregaytor environment variables (all optional; dev tooling only).
# The extension itself reads no environment variables — LLM provider keys are
# configured at runtime via the extension's settings UI, not env vars.

# Port for the local debug server (tools/debug-server). Defaults to 9222.
AGGREGAYTOR_DEBUG_PORT=9222
